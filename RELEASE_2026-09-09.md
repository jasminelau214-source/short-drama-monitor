# V1.2 Beta｜2026-09-09 发布说明

## 本版纳入内容

- 2026-09-08 四平台 Top10，共40个当日榜位。
- 9/8：7部新剧、33部继续在榜。
- 9/7 的10部待标注新剧已补入内容分析基线。
- 新增/补齐本土化字段：`localizationLevel`、`localizationJudgment`、`mismatch`。
- 新剧研究来源、置信度、待核项目写入 `research_meta.json`。
- `/collect` 采集入口已从静态壳升级为可上传原始截图至待分析区。
- 管理员认证改为 fail-closed：未配置密码时拒绝管理员入口，而不是自动放行。
- 每日榜单通过 `updates/*.json` 追加，避免覆盖历史采集记录。

## 当前系统成熟度判断

### 已可用于日常研究

1. 手工截图 → ChatGPT/人工审核 → 更新包 → GitHub → Render 公网。
2. 市场仪表盘、当日40部榜单、历史排名/生命周期展示。
3. 题材/赛道/受众/故事拆解/商业拆解/本土化字段。
4. 管理后台人工补全与 SQLite 覆盖。
5. 研究来源、置信度、待核项的基础审计记录。

### 尚未达到“全自动系统”

1. 截图上传后没有独立 OCR/parser 自动跑完。
2. 没有完整的草稿表、机器质检阻断、字段锁、review_history。
3. 没有模型 Provider Adapter 与调用预算日志。
4. 第一集与付费卡点依然依赖真实视频/人工核验。
5. Render 免费实例的 SQLite 与上传文件不是可靠持久化存储。

## 发布结论

当前版本可定义为：

> **V1.2 Beta：可公开使用的人工+AI协同研究版。**

它已经不再只是静态页面，但仍不是“上传截图后自动跑完全流程”的正式自动化版本。下一阶段应优先补持久化、草稿/审核机制和 OCR/parser，而不是继续堆前端页面。
